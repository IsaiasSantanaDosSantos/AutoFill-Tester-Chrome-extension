import cep from "cep-promise";

export const newCep = cep;

// const fakeCep = () => `${Math.floor(10000 + Math.random() * 89999)}-${Math.floor(10 + Math.random() * 89)}`;

// console.log(fakeCep());
